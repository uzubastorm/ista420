1. operators that combine rows from two query result sets (or multisets).
2. has to have same columns
3. diagram representing mathematical or logical sets
4. operator unifies the results of two input queries.
5. operator unifies the two input query results without attempting to remove duplicates from the result.
6. returns only the rows that are common to the results of the two input queries.
7. returns the number of duplicate rows matching the lower of the counts in both input multisets.
8. operator implements set differences.
9. same as the except operator but takes into account the number of occurrences of each row.
10. left to right